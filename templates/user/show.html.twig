{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container mt-8 mr-96">
    <h1 class="text-5xl py-16">{{ user.firstName }}{{ user.lastName }}</h1>


    <div class="max-w-xl rounded overflow-hidden shadow-lg py-8 ml-8 bg-[#bbd2e1] px-6 py-4">

    <table class="table">
        <tbody>

            <tr>
                <th>FirstName</th>
                <td>{{ user.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user.lastName }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ user.gender }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ user.birthDate ? user.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Ranking</th>
                <td>{{ user.ranking }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ user.address }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ user.zipcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ user.city }}</td>
            </tr>

            <img src="{{ asset('uploads/' ~ user.image) }}" alt="{{ user.image }}">

        </tbody>
    </table>


        <button>
    <a href="{{ path('app_user_index') }}">Retour à la liste</a>
            </button>
            <button>
    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
            </button>
    {{ include('user/_delete_form.html.twig') }}
    </div>
</div>
    </div>
{% endblock %}
