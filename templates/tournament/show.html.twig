{% extends 'base.html.twig' %}

{% block title %}Tournament{% endblock %}

{% block body %}

    <div class="container mt-8 mr-96">
        <h1 class="text-5xl py-16">{{ tournament.name }}</h1>

        <div class="max-w-xl rounded overflow-hidden shadow-lg py-8 ml-8 bg-[#bbd2e1] px-6 py-4">

                <table class="table">
                    <tbody>


                                <tr>
                                    <th>DateStart</th>
                                    <td>{{ tournament.dateStart ? tournament.dateStart|date('Y-m-d H:i:s') : '' }}</td>
                                </tr>
                                <tr>
                                    <th>DateEnd</th>
                                    <td>{{ tournament.dateEnd ? tournament.dateEnd|date('Y-m-d H:i:s') : '' }}</td>
                                </tr>
                                <tr>
                                    <th>Description</th>
                                    <td>{{ tournament.description }}</td>
                                </tr>
                                <tr>
                                    <th>Type</th>
                                    <td>{{ tournament.type }}</td>
                                </tr>
                                <tr>
                                    <th>PlayerNumber</th>
                                    <td>{{ tournament.playerNumber }}</td>
                                </tr>
                                <tr>
                                    <th>TableNumber</th>
                                    <td>{{ tournament.tableNumber }}</td>
                                </tr>




                                <img src="{{ asset('uploads/' ~ tournament.image) }}" alt="{{ tournament.image }}">



                    </tbody>
                </table>


                            <button>
                                <a href="{{ path('app_tournament_index') }}">Retour à la liste</a></button>
                            <button>
                                <a href="{{ path('app_tournament_show', {'id': tournament.id}) }}">modifier</a></button>

                                {{ include('tournament/_delete_form.html.twig') }}
        </div>

    </div>


    </div>
{% endblock %}
